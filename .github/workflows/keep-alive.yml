name: Keep Streamlit App Active

on:
  schedule:
    - cron: "0 */8 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -L --max-time 60 https://pressure-ulcer-classifier.streamlit.app/
